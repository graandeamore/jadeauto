//order page (not realized seems to be deleted)
import React from 'react';

const Order = () => {
    return (
        <div>
            Order
        </div>
    );
};

export default Order;